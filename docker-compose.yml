services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whale-eye-app
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
